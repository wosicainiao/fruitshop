server:
  port: 8081

spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/fruit_shop?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations: classpath:com/wc/project/fruitshop/mapper/*Mapper.xml
  type-aliases-package: com.example.entity

#showSql
logging:
  level:
    com:
      example:
        mapper: debug